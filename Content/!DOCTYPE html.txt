<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>StudyNest</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: Arial, sans-serif;
    }

    .header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 15px 20px;
      background-color: #2c3e50;
      color: white;
      position: relative;
    }

    .header img {
      height: 50px;
      width: 50px;
    }

    .header h1 {
      font-size: 1.8rem;
      flex-grow: 1;
      text-align: center;
    }

    .search-box {
      display: flex;
      gap: 5px;
    }

    .search-box input {
      padding: 8px;
      width: 180px;
      border: none;
      border-radius: 5px;
    }

    .search-box button {
      padding: 8px;
      background-color: #e74c3c;
      color: white;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }

    .search-box button:hover {
      background-color: #c0392b;
    }

    .hamburger {
      display: none;
      font-size: 1.8rem;
      cursor: pointer;
    }

    hr {
      border: 3px solid #2980b9;
    }

    #aside1 {
      display: flex;
      height: calc(100vh - 180px);
    }

    .leftsection {
      width: 20%;
      min-width: 200px;
      background: #ecf0f1;
      overflow-y: auto;
      border-right: 2px solid #2980b9;
      transition: transform 0.3s ease-in-out;
    }

    .content {
      flex-grow: 1;
      border: none;
      overflow: auto;
    }

    .leftsection a {
      display: block;
      margin: 10px;
      color: #2980b9;
      text-decoration: none;
      font-size: 18px;
      padding: 10px;
      border-bottom: 1px solid #ddd;
      cursor: pointer;
    }

    .leftsection a:hover {
      background-color: #2980b9;
      color: white;
    }

    .menu-item {
      padding: 10px;
      background-color: #3498db;
      color: white;
      cursor: pointer;
      margin: 10px;
    }

    .submenu {
      display: none;
      padding-left: 20px;
    }

    .submenu a:hover {
      color: #e74c3c;
    }

    footer {
      padding: 10px;
      text-align: center;
      font-weight: bold;
    }

    /* Responsive Styles */
    @media (max-width: 768px) {
      .header {
        flex-wrap: wrap;
        justify-content: space-between;
      }

      .hamburger {
        display: block;
      }

      #aside1 {
        flex-direction: column;
      }

      .leftsection {
        position: absolute;
        top: 75px;
        left: 0;
        width: 70%;
        height: calc(100vh - 75px);
        z-index: 1000;
        transform: translateX(-100%);
        background-color: #ecf0f1;
        border-right: 2px solid #2980b9;
      }

      .leftsection.show {
        transform: translateX(0);
      }

      .content {
        width: 100%;
        height: 60vh;
      }

      .search-box {
        width: 100%;
        margin-top: 10px;
      }

      .search-box input {
        width: 65%;
      }

      .search-box button {
        width: 30%;
      }
    }
  </style>
</head>
<body>
  <div class="header">
    <span class="hamburger" onclick="toggleMenu()">☰</span>
    <img src="Logo.png" alt="Logo" />
    <h1>StudyNest</h1>
    <div class="search-box">
      <form action="search_results.html" method="GET">
        <input type="search" name="q" placeholder="Search here..." required />
        <button type="submit">Search</button>
      </form>
    </div>
  </div>

  <hr />

  <aside id="aside1">
    <div class="leftsection" id="leftsection">
      <div class="menu-item" onclick="toggleSubmenu('html-submenu')">Computer Multimedia and Animations</div>
      <div id="html-submenu" class="submenu">
        <a href="#" onclick="loadIframe('Content/html_intro.html')">Introduction to HTML</a>
        <a href="#" onclick="loadIframe('Content/html_struct.html')">Structure of HTML</a>
        <a href="#" onclick="loadIframe('Content/html_tags.html')">HTML Tags</a>
      </div>
    </div>
    <iframe class="content" id="content" src="" frameborder="0"></iframe>
  </aside>

  <hr />

  <footer>
    <p>SMC MOODUBIDIRE</p>
  </footer>

  <hr />

  <script>
    function toggleSubmenu(id) {
      const submenu = document.getElementById(id);
      submenu.style.display = submenu.style.display === "block" ? "none" : "block";
    }

    function loadIframe(url) {
      document.getElementById('content').src = url;

      // Hide menu after clicking link (mobile only)
      const menu = document.getElementById("leftsection");
      if (window.innerWidth <= 768) {
        menu.classList.remove("show");
      }
    }

    function toggleMenu() {
      const menu = document.getElementById("leftsection");
      menu.classList.toggle("show");
    }
  </script>
</body>
</html>
